name: schedule-greeting
on:
  schedule:
    - cron: "55 12 * * *"
jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: greeting-schedule
        run: echo "welcome schedule greeting"
        
